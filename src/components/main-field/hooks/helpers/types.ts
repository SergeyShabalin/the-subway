export type CalculateNewPositionArgs = {
  pointer: { x: number; y: number }
  scale: number
  newScale: number
  position: { x: number; y: number }
}

export type NewPosition = { x: number; y: number }
